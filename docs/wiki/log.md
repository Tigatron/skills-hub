# Directory Update Log

## 2026-08-05

* **Implementation**: Completed `M0-010` with persisted Workspace Root authorization and filesystem identity; bounded hidden/ignore-aware read-only traversal; nested Git, implicit-adapter, and manual project discovery; nearest-project observation ownership; progressive safe project batches; complete-coverage-only absence; per-source diagnostics; and a live retrying `notify` invalidation/reconciliation driver for startup, resume/wake, overflow/disconnect, root replacement, and operation completion/rollback. Typed commands and generated bindings cover Workspace lifecycle and manual-project rescan.

* **Implementation**: Completed `M0-012` Vault lifecycle backend and acceptance evidence: no-overwrite external-edit reconciliation, exact-path read-only verify, conservative reviewed repair, manifest/journal index rebuild with retained backup, reference-aware two-phase GC, capability-gated relocation with managed-link repair, startup lifecycle recovery evidence, and separately confirmed old-Vault cleanup. Index rebuild and relocation intentionally require restart so live handles stay attached to the retained recovery source until reopening.

* **Implementation**: Completed `M0-011` through the existing generic scanner and Operation kernel. Added six official-source-verified versioned descriptors; persisted enablement and global/project override configuration; configured global scan-all over enabled defaults, overrides, and custom global roots; canonical-identity custom target registration/reselection; all-family mode/default/post-commit verification; and adapter-version `Unverified` invalidation without path rewriting. Generated bindings and descriptor, six-root, configuration breadth, family-mode, custom-identity, and version-transition matrices pass.

* **Implementation**: Completed `M0-009` thin-slice UI on real Rust commands. Added post-launch `vault_initialize`/`vault_status`, first-run Vault screen, Library scan/takeover/deploy plan review, Deployments fixture targets/verify/undeploy, and Activity list/detail with recovery evidence. TanStack Query invalidation follows `domain-invalidated`; no optimistic ownership. Keyboard-capable React Aria shell at 900×600. `pnpm check` passes. Breadth adapters/Workspace/Trash remain `M0-010`+.

* **Implementation**: Added the `M0-009` backend bootstrap slice: a live runtime can initialize a default or selected safe Vault after launch, atomically install scan/takeover/deployment/Activity services, run startup recovery, and expose typed Vault status/summary commands plus bootstrap Vault fields. Reinitialization is rejected, existing configured-Vault startup remains supported, generated bindings are current, and focused/full Rust tests cover before/after status, service availability, default initialization, and unsafe paths. The React thin-slice UI remains separate and M0-009 is not complete.
* **Implementation**: Completed `M0-008` after gate repair (Clippy `-D warnings`, Specta `u32` Activity diagnostic count, binding regeneration). Schema-v4 2–20 Target batch deploy with stage-all/reverse compensation, operation-level Snapshot protection, runtime startup recovery action driving that blocks mutation/scan until nonterminal journals resolve, bounded Activity list/detail with journal rebuild, and reviewed inverse undo that refuses drifted postconditions. Real-tree failpoint, 3/20-target, undo, startup driver/classifier, and scan Activity tests pass with the full `pnpm check` gate. Thin-slice UI remains `M0-009`.

## 2026-07-24

* **Implementation**: Completed `M0-007` after two independent audit/repair passes. The Universal fixture now provides ID-derived schema-v3 deploy/redeploy/undeploy plans; global, Git-project, and non-Git Target defaults and override/fallback evidence; absolute link and exact Managed Copy execution through the shared Operation kernel; manifest/SQLite/Activity finalization; Rust-owned E/V/T and link health; and clean-remove or explicit preserve undeploy. Review repairs made retargeted/replacement/missing/broken entries safely preservable without following user links, prohibited lossy non-UTF-8 plan evidence, aligned allowed actions with executable resolutions, and added undeploy rollback proof at the backup boundary. Multi-target recovery and startup action driving remain `M0-008`.

## 2026-07-23

* **Implementation**: Completed `M0-006` after two independent audit/repair passes. Takeover now provides distinct Keep external, Add to Vault, and selected-only Add and manage intents; a sealed schema-v2 plan with full source, working-container, Target-authority, replacement, and recovery evidence; verified object-backed staging; UUID working activation; exact selected replacement; manifest and critical SQLite finalization; typed Operation/Skill-detail commands; terminal replay; and conservative crash evidence. Review repairs prohibit replacing the source or its aliases, preserve full project/custom/override Target identity, keep nested-Bundle verification explicit and schema-v1-compatible, and reject Vault/target nesting.
* **Implementation**: Completed `M0-005` after independent safety review and repair. The generic Operation kernel now has canonical immutable ID-bound plans, exact destructive Snapshot registration, durable intent/action/inspection/observation journaling, stage-all and deterministic commit/verify/finalize, immediate root/parent/source/destination revalidation for every commit and rollback rename, reverse compensation with version preservation, exact cleanup, terminal replay, and a conservative startup classifier. Focused durability matrices and real parent `child.kill()` tests cover reopened backup/final/rollback journals. Product takeover/deployment builders and the M0-008 startup action driver remain planned.
* **Implementation**: Completed `M0-004`: read-only Universal global scanning, visible degraded observations, managed/unknown link classification, root-scoped complete-only stale reconciliation, duplicate active-job collapse, the external Library grouping/filter/pagination model, SQLite scanner projection v2, typed scan/Library commands, and progress/invalidation events. Focused safety review corrected invalid-candidate disappearance, case-sensitive path identity merging, concurrent stale races, and failed-run terminal persistence before the all-target, binding, frontend, docs, and `kill-ai-slop` gates passed.
* **Implementation**: Completed `M0-003`: transparent/default/custom Vault initialization and locking, versioned device/Vault/Skill/deployment manifests, checksummed strict SQLite v1 schema and dedicated executor, projection repositories and critical finalization, immutable verified object publication, and rebuild database swap utility. A focused safety review led to hot WAL/SHM swap refusal, recoverable corrupt device settings, corrected alias classification, location-key observation upserts, and copy-time caps independent of prevalidation.
* **Implementation**: Completed `M0-002`: typed UUIDv7/domain/time/path values, accepted ownership/duplicate/deployment-health and Operation state models, frozen `sha256-bundle-v1` encoder with two golden vectors and stable file/link reads, Bundle caps/statistics and link validation, nearest-existing-ancestor authorization with non-lossy path handling, and deterministic pure Operation Plan serialization/digests. A focused protocol review identified and resolved noncanonical plan verification, symlink retarget races, and active-cap propagation before completion.
* **UI correction**: Applied the project-local `kill-ai-slop` workflow to the M0 foundation screen. Removed repeated kickers, ornamental ordinals, marketing-style principles, the oversized sentence headline, cream/editorial serif styling, and a decorative footer accent; retained the flat connection dot as a documented semantic-status exception. The follow-up scan reports no unreviewed signals and the native window was visually rechecked.
* **Implementation**: Completed `M0-001`: Tauri 2/Rust 1.89/React shell, bounded Tokio blocking-work foundation, Rust-generated typed command client and drift test, React Aria/token UI foundation, fixture/failpoint harness, quality scripts, Intel compile check, real dev launch, and ad-hoc Apple Silicon `.app` packaging. Pinned the Rust-1.89-compatible Specta set after `rc.25` failed compilation on an unavailable standard-library API.
* **Acceptance**: Accepted the M0 technical baseline after resolving runtime concurrency, typed Rust/TypeScript boundaries, persistence/durability/recovery, filesystem capability and naming safety, watcher reconciliation, Vault lifecycle, UI foundation, local diagnostics, macOS/release architecture, and risk-owned testing. The consequence is that `M0-001`–`M0-017` implement fixed contracts while exact compatible package versions and current official agent paths remain implementation-time facts; at acceptance time all implementation tasks were still planned.
* **Completion**: Added the breadth (`M0-010`–`M0-014`) and release (`M0-015`–`M0-017`) task pages and the full M0 traceability matrix covering all 43 requirements and 12 acceptance criteria. Assigned previously unowned requirements: VLT-05..08 to `M0-012`, SCN-05..08 to `M0-010`, IMP-08 to `M0-015` (matching the `M0-006` exclusion), DPL-12 to `M0-014`, and DEL-02..05 to `M0-013`.
* **Initialization**: Established the OKF v0.1 technical Wiki and repository documentation rules.
* **Creation**: Added the proposed M0 architecture, domain, storage, workflow, interface, security, and quality concepts.
* **Planning**: Added the M0 dependency roadmap, executable task breakdown, and requirement-to-task traceability matrix.
